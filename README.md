This repository is a preliminary test of the sn26 metric for learning an RL policy for navigation.

Currently running the main.py trains two models: one without the sn26 reward and one with and then tests both of them. In order to test the trained models again, change the train flag to false in the main program.
